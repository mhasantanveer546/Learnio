# Pomodoro timing — fixed constants for Phase 11 v1. If custom
# durations get added later, only this file (and the JS constants
# mirroring it) need to change — the timer logic itself doesn't care
# where these numbers come from.
POMODORO_FOCUS_MINUTES = 25
POMODORO_BREAK_MINUTES = 5